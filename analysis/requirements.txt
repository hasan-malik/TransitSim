# TransitSim — analysis layer (Bayesian calibration, validation, inverse optimization)
# Python 3.11+ recommended.  Install:  pip install -r requirements.txt
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
pymc>=5.10
arviz>=0.17
cvxpy>=1.4
jupyter>=1.0
